runtime: python
target: remote
url: https://xrpl-referee.onrender.com/mcp/

configSchema:
  type: object
  # We leave properties empty because the server is configured via 
  # environment variables on the host (Render).
  properties: {}
  required: []
  additionalProperties: false

exampleConfig: {}

# Metadata to help Smithery categorize and rank the server
categories:
  - blockchain
  - crypto
  - ai
  - finance
