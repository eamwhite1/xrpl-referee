{
  "openapi": "3.1.0",
  "info": {
    "title": "AgentTrust Referee",
    "description": "A trustless AI verdict engine for the agent economy. POST a task spec and proof of work, pay $0.10 (XRP, RLUSD, or USDC), receive a structured PASS/FAIL verdict. Optionally use the full escrow protocol via /escrow/generate to lock and release XRPL funds automatically on approval.",
    "version": "7.0.0",
    "x-ai-service-type": "auditor",
    "x-payment-requirement": {
      "currency": "XRP",
      "amount": "0.1",
      "destination": "rmcSrkpZ2i2kuvtCPeTVetee9SixP4djR",
      "description": "Send $0.10 (XRP or RLUSD) to this address before calling /audit. Pass the transaction hash in the x-payment-hash header."
    }
  },
  "servers": [
    {
      "url": "https://xrpl-referee.onrender.com",
      "description": "Production — XRPL Mainnet"
    }
  ],
  "paths": {
    "/audit": {
      "post": {
        "summary": "Standalone AI verdict — no escrow required",
        "description": "The simplest integration. Pay $0.10 (XRP, RLUSD, or USDC), get a structured AI verdict on whether work meets spec. No vault, no receipt code, no XRPL escrow needed. Use /escrow/generate if you want funds locked and released automatically.",
        "operationId": "standaloneAudit",
        "parameters": [
          {
            "name": "x-payment-hash",
            "in": "header",
            "description": "XRPL/Base transaction hash of your $0.10 payment to rmcSrkpZ2i2kuvtCPeTVetee9SixP4djR",
            "required": false,
            "schema": { "type": "string" }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["task", "work"],
                "properties": {
                  "task": {
                    "type": "string",
                    "description": "The original task specification or instructions given to the worker.",
                    "maxLength": 5000
                  },
                  "work": {
                    "type": "string",
                    "description": "The completed work or output produced by the worker.",
                    "maxLength": 5000
                  },
                  "fee_hash": {
                    "type": "string",
                    "description": "Alternative to x-payment-hash header. XRPL/Base tx hash of $0.10 payment."
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Verdict returned",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status":           { "type": "string", "enum": ["approved", "rejected"] },
                    "verdict":          { "type": "string", "enum": ["PASS", "FAIL"] },
                    "score":            { "type": "number", "description": "0.0–1.0 confidence score" },
                    "summary":          { "type": "string", "description": "One-sentence verdict summary" },
                    "details":          { "type": "string", "description": "Full reasoning from the AI" },
                    "criteria_met":     { "type": "array", "items": { "type": "string" } },
                    "criteria_failed":  { "type": "array", "items": { "type": "string" } },
                    "model_used":       { "type": "string" }
                  }
                }
              }
            }
          },
          "402": { "description": "Payment required — fee hash missing or invalid" },
          "403": { "description": "Forbidden — payment hash already used (anti-replay)" }
        }
      }
    },
    "/escrow/generate": {
      "post": {
        "summary": "Create an AI-gated XRPL escrow vault",
        "description": "Locks funds in XRPL crypto-condition escrow. The fulfillment key is only released when /evaluate approves the submitted work. Use this for the full trustless payment flow.",
        "operationId": "generateEscrow",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["escrow_id", "fee_hash", "buyer_name", "buyer_address", "task_description", "worker_address", "amount_xrp"],
                "properties": {
                  "escrow_id":        { "type": "string", "description": "Unique receipt code e.g. AT-7X9K-2MQ4" },
                  "fee_hash":         { "type": "string", "description": "XRPL/Base tx hash of $0.10 protocol fee" },
                  "project_label":    { "type": "string", "description": "Human-readable job label e.g. Job-01" },
                  "buyer_name":       { "type": "string" },
                  "buyer_address":    { "type": "string", "description": "Buyer's XRPL wallet address" },
                  "task_description": { "type": "string" },
                  "worker_address":   { "type": "string", "description": "Worker's XRPL wallet — receives funds on approval" },
                  "amount_xrp":       { "type": "number" },
                  "cancel_after_hrs": { "type": "integer", "default": 168 }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Vault created",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "escrow_id":           { "type": "string" },
                    "condition":           { "type": "string", "description": "SHA-256 crypto-condition for EscrowCreate tx" },
                    "cancel_after_ripple": { "type": "integer" },
                    "cancel_after_human":  { "type": "string" }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/evaluate": {
      "post": {
        "summary": "Submit work proof for escrow-linked AI audit",
        "description": "Worker submits proof against an existing vault. On PASS, returns the fulfillment key needed to complete EscrowFinish and claim payment.",
        "operationId": "evaluateWork",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["escrow_id", "work"],
                "properties": {
                  "escrow_id": { "type": "string", "description": "Receipt code from /escrow/generate" },
                  "work":      { "type": "string", "description": "Completed work or proof of completion" }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Audit result",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status":          { "type": "string", "enum": ["approved", "rejected"] },
                    "fulfillment":     { "type": "string", "description": "Fulfillment hex — submit in EscrowFinish (PASS only)" },
                    "condition":       { "type": "string", "description": "Condition hex — required in EscrowFinish" },
                    "buyer_address":   { "type": "string" },
                    "escrow_sequence": { "type": "integer" },
                    "amount_xrp":      { "type": "number" },
                    "verdict":         { "type": "object" }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/kyc/verify": {
      "post": {
        "summary": "Register Xaman KYC for a wallet",
        "description": "Checks whether a wallet has completed Xaman identity verification and caches the result. Call this after completing KYC in the Xaman app. Once verified, the wallet can create escrows up to $10,000 (vs. the default $3,000 cap). AgentTrust never stores identity documents — only the pass/fail status from the Xaman platform.",
        "operationId": "kycVerify",
        "parameters": [
          {
            "name": "wallet_address",
            "in": "query",
            "required": true,
            "schema": { "type": "string" },
            "description": "The XRPL wallet address to check and register KYC status for."
          }
        ],
        "responses": {
          "200": {
            "description": "KYC status for the wallet",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "wallet_address": { "type": "string" },
                    "kyc_verified":   { "type": "boolean" },
                    "method":         { "type": "string", "example": "xaman" },
                    "message":        { "type": "string" },
                    "xaman_kyc_url":  { "type": "string" }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/kyc/status/{wallet_address}": {
      "get": {
        "summary": "Check KYC verification status",
        "description": "Returns the KYC verification status for a wallet address. Checks the AgentTrust cache first, then queries Xaman live if no cached record exists.",
        "operationId": "kycStatus",
        "parameters": [
          {
            "name": "wallet_address",
            "in": "path",
            "required": true,
            "schema": { "type": "string" },
            "description": "The XRPL wallet address to check."
          }
        ],
        "responses": {
          "200": {
            "description": "KYC status",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "wallet_address": { "type": "string" },
                    "kyc_verified":   { "type": "boolean" },
                    "method":         { "type": "string" },
                    "verified_at":    { "type": "string", "format": "date-time" }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/status": {
      "get": {
        "summary": "Health check",
        "operationId": "healthCheck",
        "responses": {
          "200": {
            "description": "Service online",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status":    { "type": "string" },
                    "version":   { "type": "string" },
                    "timestamp": { "type": "string" }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
